[package]
name = "structury-json"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "JSON demand codec: RFC 8259, NDJSON, JSON-seq"
readme = "README.md"
keywords = ["json", "codec", "demand"]
categories = ["encoding", "no-std"]

[dependencies]
structury = { workspace = true, features = ["byte-scan"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
